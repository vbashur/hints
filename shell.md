# Hints to various shell commands


Apply changes made in .zshrc (re-read the file)\
`. ~/.zshrc`


Find the occurence of the search term in all files of the specified directory
`grep -rnw '/path/to/somewhere/' -e 'pattern'`
